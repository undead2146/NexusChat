<?xml version="1.0" encoding="UTF-8"?>
<?xaml-comp compile="true"?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NexusChat.Views.Controls">

    <!-- Font Awesome Resources -->
    <FontImageSource x:Key="IconBack" 
                     FontFamily="FontAwesome-Solid" 
                     Glyph="&#xf053;" 
                     Size="18" 
                     Color="{AppThemeBinding Light=#333333, Dark=#ffffff}" />

    <FontImageSource x:Key="IconSearch" 
                     FontFamily="FontAwesome-Solid" 
                     Glyph="&#xf002;" 
                     Size="16" 
                     Color="{AppThemeBinding Light=#666666, Dark=#cccccc}" />

    <FontImageSource x:Key="IconStar" 
                     FontFamily="FontAwesome-Solid" 
                     Glyph="&#xf005;" 
                     Size="20" 
                     Color="#FFD700" />

    <FontImageSource x:Key="IconRefresh" 
                     FontFamily="FontAwesome-Solid" 
                     Glyph="&#xf021;" 
                     Size="16" 
                     Color="{AppThemeBinding Light=#333333, Dark=#ffffff}" />

    <!-- Common Button Styles -->
    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Primary}}" />
        <Setter Property="WidthRequest" Value="40" />
        <Setter Property="HeightRequest" Value="40" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="Padding" Value="0" />
    </Style>
    
    <Style x:Key="CardButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="HeightRequest" Value="30" />
    </Style>
    
    <!-- Badge Styles -->
    <Style x:Key="PrimaryBadgeStyle" TargetType="Border">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#E3F2FD, Dark=#0D47A1}" />
        <Setter Property="StrokeThickness" Value="0" />
        <Setter Property="StrokeShape" Value="RoundRectangle 3" />
        <Setter Property="Padding" Value="4,1" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>
    
    <Style x:Key="SuccessBadgeStyle" TargetType="Border">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#E8F5E9, Dark=#1B5E20}" />
        <Setter Property="StrokeThickness" Value="0" />
        <Setter Property="StrokeShape" Value="RoundRectangle 3" />
        <Setter Property="Padding" Value="4,1" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>
    
    <!-- Frame Styles -->
    <Style x:Key="CardFrameStyle" TargetType="Frame">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}" />
        <Setter Property="BorderColor" Value="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource BorderColorDark}}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="HasShadow" Value="False" />
        <Setter Property="Padding" Value="12" />
    </Style>

    <!-- Common Styles -->
    <Style x:Key="HeaderStyle" TargetType="Label">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{AppThemeBinding Light=#333333, Dark=#ffffff}" />
    </Style>

    <Style x:Key="SubHeaderStyle" TargetType="Label">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{AppThemeBinding Light=#555555, Dark=#cccccc}" />
    </Style>

    <Style x:Key="BodyTextStyle" TargetType="Label">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextColor" Value="{AppThemeBinding Light=#666666, Dark=#bbbbbb}" />
    </Style>

    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FFFFFF, Dark=#2D2D2D}" />
        <Setter Property="StrokeShape" Value="RoundRectangle 8,8,8,8" />
        <Setter Property="Stroke" Value="{AppThemeBinding Light=#e0e0e0, Dark=#555555}" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Margin" Value="8" />
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#0078D7, Dark=#0078D7}" />
        <Setter Property="TextColor" Value="White" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Padding" Value="16,8" />
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{AppThemeBinding Light=#0078D7, Dark=#0078D7}" />
        <Setter Property="BorderWidth" Value="1" />
        <Setter Property="TextColor" Value="{AppThemeBinding Light=#0078D7, Dark=#0078D7}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="16,8" />
    </Style>

</ResourceDictionary>
